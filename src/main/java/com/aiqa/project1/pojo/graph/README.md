# pojo.graph

本目录存放图流程相关的简单状态对象。

## 主要文件

- `SimpleState`: 用于图执行或流程测试的轻量状态载体。

## 使用场景

该目录与 `pojo/nodes` 的 RAG 图状态对象相比更轻量，适合保存简单流程或示例图的状态。

## 维护提示

- 如果状态对象开始承载 RAG 业务字段，应优先考虑移动或合并到 `pojo/nodes`，避免图状态分散。
- 状态字段需要保持可序列化，方便异步执行、日志记录和调试。
